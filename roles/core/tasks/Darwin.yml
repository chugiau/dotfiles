---
# On macOS, openssh is built-in. Install the rest via Homebrew.
- name: "core | Darwin | Install core packages via Homebrew"
  community.general.homebrew:
    name:
      - git
      - git-lfs
      - jq
      - gnupg
      - curl
      - wget
    state: present
