---
# On macOS, always use Homebrew regardless of pm_preference
- name: "cli_tools | Darwin | Install CLI tools via Homebrew"
  community.general.homebrew:
    name:
      - bat
      - eza
      - lazygit
      - glow
    state: present
