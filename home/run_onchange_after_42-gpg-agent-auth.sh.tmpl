#!/bin/sh
# Restart gpg-agent when the managed auth unlock config changes.
#
# config-hash: {{ include "private_dot_gnupg/gpg-agent.conf.tmpl" | sha256sum }}

set -eu

if command -v gpgconf >/dev/null 2>&1; then
    gpgconf --kill gpg-agent >/dev/null 2>&1 || true
fi
